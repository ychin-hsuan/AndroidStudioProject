<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:padding="12dp"
    android:gravity="center_vertical"
    android:background="?attr/selectableItemBackground"
    android:clickable="true"
    android:focusable="true">

    <!-- 日期區塊 -->
    <LinearLayout
        android:layout_width="52dp"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center"
        android:layout_marginEnd="12dp">

        <TextView
            android:id="@+id/tv_week_day_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="週一"
            android:textSize="11sp"
            android:textColor="@color/text_secondary"
            android:gravity="center"/>

        <TextView
            android:id="@+id/tv_week_day_num"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:text="24"
            android:textSize="16sp"
            android:textColor="@color/text_primary"
            android:fontFamily="sans-serif-medium"
            android:gravity="center"
            android:layout_marginTop="2dp"/>
    </LinearLayout>

    <!-- 空檔進度條 -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical"
        android:layout_marginEnd="10dp">

        <ProgressBar
            android:id="@+id/progress_free"
            style="@style/Widget.AppCompat.ProgressBar.Horizontal"
            android:layout_width="match_parent"
            android:layout_height="5dp"
            android:max="100"
            android:progress="60"
            android:progressTint="@color/planck_green"
            android:progressBackgroundTint="@color/planck_purple_light"
            android:layout_marginBottom="4dp"/>

        <!-- 事件 Pills -->
        <LinearLayout
            android:id="@+id/layout_event_pills"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"/>

    </LinearLayout>

    <!-- 空檔時數 -->
    <TextView
        android:id="@+id/tv_free_hours"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="6h"
        android:textSize="13sp"
        android:textColor="@color/planck_green"
        android:fontFamily="sans-serif-medium"/>

</LinearLayout>